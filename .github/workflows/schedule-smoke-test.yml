name: Schedule Smoke Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print schedule smoke test
        run: |
          echo "Schedule smoke test executed"
          date -u
